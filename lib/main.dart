import 'package:flutter_starter_kit/bootstrap.dart';

/// Default entry point. Uses --dart-define=ENV=dev|staging|prod for environment.
/// For flavor-based builds, use main_dev.dart, main_staging.dart, or main_prod.dart.
Future<void> main() => bootstrap();
